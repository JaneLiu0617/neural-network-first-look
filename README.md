# neural-network-first-look

Install the R packages kera and tensorflow for use with a CPU. Run the cars

Example. Run the concrete Example. Run the iris Example. Run the code in Chapter 2. A first look at a
neural network
